const protocol = "https";
const host = "gmail.api.semicolonapi.com";
const port = "";
const trailUrl = "";
const hostUrl = process.env.REACT_APP_HOSTURL
const endpoint = process.env.REACT_APP_ENDPOINT
// const endpoint = "http://192.168.29.218:5001";

const config = {
  protocol: protocol,
  host: host,
  port: port,
  apiUrl: trailUrl,
  endpoint: endpoint,
  hostUrl: hostUrl,
};
export default config;
